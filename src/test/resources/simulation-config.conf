presets {
  standard {
    tierCount = 6
    tierDurationMinutes = 30
    userIncrementPerTier = 32
  }

  commit {
    tierCount = 1
    tierDurationMinutes = 1
    userIncrementPerTier = 20
  }

  pull_request {
    tierCount = 1
    tierDurationMinutes = 1
    userIncrementPerTier = 20
  }

  dev {
    tierCount = 1
    tierDurationMinutes = 1
    userIncrementPerTier =  10
    tierCount = ${?TIER_COUNT} # Defaults are provided, but overridden if corresponding environment variables are set
    tierDurationMinutes =  ${?TIER_DURATION_MINUTES}
    userIncrementPerTier =  ${?USER_INCREMENT_PER_TIER}
  }
}